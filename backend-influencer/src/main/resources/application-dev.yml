spring:
  config:
    activate:
      on-profile: dev
  
  datasource:
    master:
      url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:hongrenfuwu}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: ${DB_USER:root}
      password: ${DB_PASSWORD:Yaoxingxuanlv-39}
      driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:hongrenfuwu2025}

# 本地开发 CORS 配置
app:
  cors:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173,http://localhost:8082
  storage:
    type: MINIO
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: InfluencerMinio2025
    base-path: influencer-media
    public-url-prefix: http://localhost:9000/influencer-media

services:
  eccang-integration:
    url: http://localhost:8081
  user:
    url: http://localhost:8080

logging:
  level:
    root: INFO
    com.athlunakms.influencer: INFO
